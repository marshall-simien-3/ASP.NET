C# for Interview


private int FindWeekNumber()
    {
        DateTimeFormatInfo dfi = DateTimeFormatInfo.CurrentInfo;
        System.Globalization.Calendar cal = dfi.Calendar;
        return cal.GetWeekOfYear(nowDT, dfi.CalendarWeekRule, dfi.FirstDayOfWeek);
    }//end FindWeekNumber()
